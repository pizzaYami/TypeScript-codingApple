let 문자: string = "kim";
let 숫자: number = 50;
let 참거짓: boolean = true;

let 배열1: number[] = [1, 2, 3];
let 배열2: string[] = ["a", "b", "c"];
let 배열3: (number | string)[] = [1, "a", 2, "b"];

let 객체1: { 사과: string; 포도: string } = { 사과: "apple", 포도: "grape" };
let 객체2: { age: number; koreaAge: number } = { age: 14, koreaAge: 16 };

let 이름: string = "조용주";
let 나이: number = 29;
let 출생지역: string = "경기도 이천";

let favoriteSong: { singer: string; song: string } = {
  singer: "장범준",
  song: "고백",
};

let project: {
  member: string[];
  days: number;
  started: boolean;
} = {
  member: ["kim", "park"],
  days: 30,
  started: true,
};
